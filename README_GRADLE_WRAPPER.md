# Gradle wrapper

このプロジェクトは **Gradle 8.13** を wrapper で固定しています。

- Windows: `app\gradlew.bat` / Android Studio が wrapper を使用
- macOS/Linux: `./app/gradlew`

初回だけ `~/.gradle/wrapper/dists` に Gradle 本体がダウンロードされます。
